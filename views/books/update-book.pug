extends ../layout

block content
  h1= title
  form(action=`/books/${book.id}`, method="post")
    include errors
    include form
  
  form(action=`/books/${book.id}/delete`, method="post" onsubmit=`return confirm('Are you sure you want to delete "${book.title}"')` )
    p
      a.button(href="/books") Cancel 
    p
      input(type="submit" value="Delete Book" )
 
  
  